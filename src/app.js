import Windowed from "./Windowed"
import '../assets/style.css'
import '@fortawesome/fontawesome-free/css/all.css'
customElements.define('app-windowed', Windowed)
// document.addEventListener('click', (event) => {

// console.log(event)
//     console.table({x: event.screenX, y: event.screenY})
//     console.table({x: event.clientY, y: event.clientY})
// })